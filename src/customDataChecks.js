/*var validators = {
  length: function(arr){
    return arr.length < 13;
  },
  duration: function(str){
    str = str.match(/\d+ms{1}|\d+s{1}/g);
    return str.length;
  },
  uniq: function(obj){

  }
};*/